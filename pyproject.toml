[project]
name = "GHWs"
version = "1.02"
description = "A Sage package for computing the generalized Hamming weights of a linear code"
authors = [{ name = "Rodrigo San-José" }]
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["GHWs"]
